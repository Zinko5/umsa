Resuelve el siguiente problema, usando el lenguaje de programacion que consideres optimo, sin comentar el codigo en ningun lugar, tampoco expliques como funciona, solo devuelve: el codigo, y la salida que daria el codigo una vez ejecutado el ejemplo de entrada, compara si la salida obtenida es igual a la salida de ejemplo con la misma entrada de ejemplo.
Ademas de no comentar el codigo, que los nombres de las variables esten en español, abreviadas y en camelCase. La entrada debe ser ingresada por teclado y la salida debe mostrarse en el mismo formato que la salida de ejemplo.
Ten en cuenta que la entrada puede ser muy larga 

traduce ese mismo codigo a java; sin usar ninguna estructura muy compleja de java, que el codigo sea capaz de entenderlo cualquier principiante en java, sin usar cosas como funciones lambda, tampoco el operador ternario o condicional ni bucles for each (si usas bucles for, que sean for i), completando a partir de:

    public class e2905 {
        public static void main(String[] args) {
            
        }
    }