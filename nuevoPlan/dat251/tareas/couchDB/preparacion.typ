#set text(lang:"es")
#set text(font: "DejaVu Sans")
#show math.equation: set text(font: "DejaVu Math TeX Gyre")

Introduccion a CouchDB
historia de CouchDB
Colecciones, nodos, documentos?
Su CAP
que interfaz utiliza CouchDB?
que es CouchDB CLI y como funciona (es es curl)
que es Fauxton y como funciona (que esl el \_rev que se genera al crear un documento)
que es query mango (explicar que no existen joins, es una limitacion de mongodb)
pros y contras de CouchDB
que aplicaciones o programas utilizan CouchDB?

instalación (mencionar que es recomendable poner de usuario admin y contraseña 123, o sino tendran que cambiarlo en los comandos)
muestra de uso con Fauxton
muestra de uso con CLI
importacion de db con cli
muestra de la base de datos importada en Fauxton
consultas con CLI 
consultas con Mango Queries